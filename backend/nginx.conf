server {
    listen 80;

    server_name ~^(?<project>.+)\.deploy\.run\.place$;

    root /app/projects/$project;

    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
